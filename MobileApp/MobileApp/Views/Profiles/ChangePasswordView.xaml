<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:sh="clr-namespace:Sharpnado.Shades;assembly=Sharpnado.Shadows" xmlns:controls="clr-namespace:MobileApp.Controls"
             mc:Ignorable="d"
             x:Class="MobileApp.Views.Profiles.ChangePasswordView">
    <ContentPage.Content>
        <sh:Shadows Style="{StaticResource card}" Grid.Row="1" CornerRadius="20" >
            <Frame Style="{StaticResource  entryFrameStyle }" CornerRadius="20">
                <StackLayout  Padding="20" Grid.Row="1">
                    <StackLayout Orientation="Horizontal">
                        <FlexLayout >
                            <Label Margin="5,0,0,0" Text="&#xf084;" 
                               FontFamily="{StaticResource FontAwesomeSolid}"  FontSize="Medium" FontAttributes="Bold"/>
                            <Label Margin="5,0,0,0" Text="UBAH PASSWORD"  FontSize="Small" FontAttributes="Bold"/>
                        </FlexLayout>
                        <Label HorizontalOptions="EndAndExpand" Text="&#xf057;" 
                               FontFamily="{StaticResource FontAwesomeSolid}" FontSize="20" >
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="closetap"></TapGestureRecognizer>
                            </Label.GestureRecognizers>
                        </Label>
                    </StackLayout>
                    <Label Margin="5,20,0,0" Text="Password" Style="{StaticResource dataTitle}"/>
                    <Frame Style="{StaticResource entryFrameStyle}">
                        <sh:Shadows Style="{StaticResource inputShadow}"  BackgroundColor="{StaticResource MainColor}">
                            <controls:EntryCustom Placeholder="Password" Text="{Binding OldPassword}" IsPassword="True" FontSize="Small" CornerRadius="10" HeightRequest="35" Margin="10" ></controls:EntryCustom>
                        </sh:Shadows>
                    </Frame>

                    <Label Margin="5,10,0,0" Text="Password Baru" Style="{StaticResource dataTitle}"/>
                    <Frame Style="{StaticResource entryFrameStyle}">
                        <sh:Shadows Style="{StaticResource inputShadow}"  BackgroundColor="{StaticResource MainColor}">
                            <controls:EntryCustom Placeholder="Password Baru" Text="{Binding NewPassword}" IsPassword="True" FontSize="Small" CornerRadius="10" HeightRequest="35" Margin="10" ></controls:EntryCustom>
                        </sh:Shadows>
                    </Frame>

                    <Label Margin="5,10,0,0" Text="Confirm Password" Style="{StaticResource dataTitle}"/>
                    <Frame Style="{StaticResource entryFrameStyle}">
                        <sh:Shadows Style="{StaticResource inputShadow}"  BackgroundColor="{StaticResource MainColor}">
                            <controls:EntryCustom Placeholder="Confirm Password" Text="{Binding ConfirmPassword}" IsPassword="True" FontSize="Small" CornerRadius="10" HeightRequest="35" Margin="10" ></controls:EntryCustom>
                        </sh:Shadows>
                    </Frame>
                    <ActivityIndicator  Margin="10" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="OrangeRed" HorizontalOptions="CenterAndExpand"/>

                    <Label Margin="5,10,0,0" Text="{Binding ErrorMessage}" Style="{StaticResource dataTitle}" TextColor="Red"/>
                    <sh:Shadows Grid.Row="1" Margin="0,20,0,10" CornerRadius="10" 
                                Shades="{sh:NeumorphismShades}">
                        <Frame Style="{StaticResource entryFrameStyle}">
                            <Button Style="{StaticResource okButton}" CornerRadius="10" Text="Simpan"  Command="{Binding SaveCommand}" />
                        </Frame>
                    </sh:Shadows>

                </StackLayout>
            </Frame>
        </sh:Shadows>

    </ContentPage.Content>
</ContentPage>