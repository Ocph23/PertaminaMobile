<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:sh="clr-namespace:Sharpnado.Shades;assembly=Sharpnado.Shadows"
             xmlns:controls="clr-namespace:MobileApp.Controls"
             mc:Ignorable="d"
             x:Class="MobileApp.Views.LoginView">
    <ContentPage.Resources>
        <Style ApplyToDerivedTypes="True" TargetType="sh:Shadows">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Shades">
                <sh:ImmutableShades>
                    <sh:Shade BlurRadius="10"
                      Opacity="1"
                      Offset="-10,-10"
                      Color="White" />
                    <sh:Shade BlurRadius="10"
                      Opacity="1"
                      Offset="6, 6"
                      Color="#19000000" />
                </sh:ImmutableShades>
            </Setter>
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="25*"/>
                <RowDefinition Height="70*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0">
                <Image Source="logo.png" Background="Gray" HorizontalOptions="Center" VerticalOptions="EndAndExpand" WidthRequest="100" HeightRequest="100">
                    <Image.Clip>
                        <EllipseGeometry Center="75,75" RadiusX="50" RadiusY="50"/>
                    </Image.Clip>
                </Image>
            </StackLayout>
            <StackLayout Grid.Row="1">
                <StackLayout Margin="50">
                    
                    <Label Margin="5,40,0,0" Text="User Name" Style="{StaticResource dataTitle}"/>
                    <sh:Shadows Grid.Row="1"
                    Grid.Column="1"
                    CornerRadius="10"
                    Shades="{sh:NeumorphismShades}">
                        <controls:EntryCustom Placeholder="User Name"  TextColor="Black"  BackgroundColor="White"></controls:EntryCustom>
                    </sh:Shadows>
                    <Label Margin="5,20,0,0" Text="Password"  Style="{StaticResource dataTitle}"/>
                    <sh:Shadows  CornerRadius="10" Margin="20" Padding="10" Shades="{sh:NeumorphismShades}">
                        <controls:EntryCustom Background="#F1F1F1"></controls:EntryCustom>
                    </sh:Shadows>
                    <Entry  Placeholder="Password" Text="{Binding Model.Password}" Style="{StaticResource entryStyle}" IsPassword="True"/>
                    <Label FontSize="Small" TextColor="OrangeRed" Text="{Binding Model.ErrorMessage}"/>
                    <Button Style="{StaticResource okButton}" Text="Login"  Command="{Binding LoginCommand}" />
                    <Label FontSize="Small" TextColor="LightBlue" Text="Lupa Password">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ForgotPasswordCommand}"/>
                        </Label.GestureRecognizers>
                    </Label>
                    <Label FontSize="Small"  TextColor="LightBlue" Text="Registrasi">
                        <Label.GestureRecognizers>

                            <TapGestureRecognizer Command="{Binding RegistrasiCommand}"/>
                        </Label.GestureRecognizers>
                    </Label>
                    <Entry Style="{StaticResource entryStyle}" VerticalOptions="EndAndExpand" HorizontalTextAlignment="Center" Text="URL"/>
                </StackLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>