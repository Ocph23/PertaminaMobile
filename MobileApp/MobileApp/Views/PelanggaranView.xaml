<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sh="clr-namespace:Sharpnado.Shades;assembly=Sharpnado.Shadows"
              NavigationPage.HasNavigationBar="False"
             BackgroundColor="{StaticResource MainColor}"
             mc:Ignorable="d"
             x:Class="MobileApp.Views.PelanggaranView">
    <ContentPage.Content>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <sh:Shadows Style="{StaticResource card}" CornerRadius="25">
                    <Frame WidthRequest="45" HeightRequest="50" Padding="5"  Margin="0" BorderColor="White"  
                           CornerRadius="25" Style="{StaticResource entryFrameStyle}" BackgroundColor="White">
                        <Image Source="logo.png" Aspect="AspectFill" Margin="5" />
                    </Frame>
                </sh:Shadows>
                <StackLayout Grid.Column="1" Margin="0,10">
                    <Label VerticalOptions="Center" Text="Hello," Margin="0"/>
                    <Label VerticalOptions="Center" FontSize="Medium" Text="Candra Wijaksana"/>
                </StackLayout>
                <StackLayout Grid.Column="2" WidthRequest="100">
                    <Label Text="&#xf00d;" Style="{StaticResource Icons}" Margin="10" FontSize="Medium" VerticalOptions="Start" HorizontalOptions="End" />
                </StackLayout>
            </Grid>
            <sh:Shadows Grid.Row="1" Style="{StaticResource card}">
                <Frame Style="{StaticResource entryFrameStyle}">
                    <ListView Grid.Row="1" BackgroundColor="Transparent" IsPullToRefreshEnabled="True" IsRefreshing="{Binding IsBusy}" RefreshCommand="{Binding LoadCommand}"  
                              ItemSelected="onSelected"
                              ItemsSource="{Binding SourceView}"
                              HasUnevenRows="True" SelectionMode="Single" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <sh:Shadows Style="{StaticResource card}" >
                                        <Frame CornerRadius="10" Style="{StaticResource entryFrameStyle}" Padding="10" >
                                            <Grid >
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Image Source="noimage.png" HeightRequest="100" WidthRequest="100"  />
                                                
                                                <StackLayout Grid.Column="1">
                                                    <Label Text="10/10/2020" FontSize="Micro"/>
                                                    <StackLayout Padding="10">
                                                        <Label Text="{Binding .}" FontSize="Default" FontAttributes="Bold"></Label>
                                                        <Label Text="Anda Telah Melakukan pelanggaran "/>
                                                    </StackLayout>
                                                </StackLayout>
                                                <StackLayout Grid.Column="2">
                                                    <Label Text="-0.3" FontSize="Micro"/>
                                                </StackLayout>
                                            </Grid>
                                        </Frame>
                                    </sh:Shadows>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Frame>
            </sh:Shadows>
        </Grid>

    </ContentPage.Content>
</ContentPage>